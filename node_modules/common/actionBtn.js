'use strict'

var $ = require('jquery')

var handleList = {}

var actionBtn = (function () {
  function init () {
    $(document).on('click', '.action', function () {
      var action = $(this).attr('action')
      handleList[action](this)
    })
  }
  
  init()
  
  function _extend (str, fn) {
    var item = ''
    if (typeof str !== 'string') {
      for (item in obj) {
        handleList[item] = obj[item]
      }
    } else {
      handleList[str] = fn
    }
  }
  
  return {
    extend: _extend
  }
}())

module.exports = actionBtn